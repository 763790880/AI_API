# AI_API 项目协作规范

本文件是 AI_API 仓库的项目级工作约束，适用于后续代码修改、合并、测试和发布。

## 1. 项目主体与参考项目

- 本仓库对应线上项目 **AI_API**，AI_API 的现有代码、配置、数据模型和运行行为是唯一主体。
- `PixelAPI` 只能作为参考示例，用于比较计费、账号共享、界面或实现思路；不得把 PixelAPI 当作 AI_API 的主体，也不得直接用 PixelAPI 整体覆盖 AI_API。
- 参考代码迁移时必须先确认版本、依赖、数据结构和调用链兼容性。OpenAI 转发、账号、计费、错误事件、响应结构等相关文件必须按同一版本成组迁移，禁止混用不同版本的单文件实现。
- 发现参考项目与 AI_API 的模块、字段或接口不一致时，优先保留 AI_API 的契约，并新增适配层或做最小修改；不得为消除单个编译错误而连续拼接不兼容代码。

## 2. 功能与产品边界

- 客户账号共享功能默认关闭，仅对明确授权的客户开启；开关、权限和计费逻辑必须与普通客户隔离。
- 客户上架账号、抽成比例、账号共享等功能应沿用 AI_API 的权限和计费模型；PixelAPI 的实现仅作参考，不能改变 AI_API 的默认业务规则。
- 导航项的隐藏必须通过明确的系统设置或权限控制实现。删除/隐藏 Discord、“我的账号”等入口时，同时检查路由、权限、接口和前端空状态，避免只删菜单造成死链。
- 金额展示使用人民币符号 `￥`；代码中的 shell 提示符、美元计价协议、第三方 API 字段名和数据库历史值不因界面改动而被误改。

## 3. 修改前的检查

- 先确认当前仓库、分支、远程地址和工作区状态；保留用户已有改动，不得使用破坏性重置覆盖未提交内容。
- 仓库存在 `.codegraph/` 时，优先使用 CodeGraph 定位符号、调用链和文件；必要时再使用文本搜索。
- 修改前阅读实际执行路径、相关配置和测试，不以历史记录、计划文档或参考项目代码推断当前行为。
- 涉及架构、依赖、权限、数据迁移或删除数据的改动，先说明影响范围和风险，确认后再执行。

## 4. 构建、测试与发布门槛

- “代码已修改”“本地构建通过”“容器构建通过”“服务健康”“浏览器可访问”“已发布”是不同状态，必须分别报告，不能互相替代。
- 至少完成受影响模块的静态检查和最窄相关测试；涉及 Go 后端、前端或 Docker 时，分别验证对应构建链路。
- 发布前必须确认：构建成功、数据库和 Redis 配置可用、容器健康检查通过、关键页面可访问、核心登录/转发路径没有明显错误。
- 若验证被环境、凭据、网络或第三方服务阻塞，应明确写出阻塞证据和未验证范围，不得声称“已发布”或“已修复”。
- 线上部署默认保持 AI_API 当前稳定服务；未经明确确认，不切换到 PixelAPI 镜像，不重置数据库，不删除线上数据。
- 推送和发布必须只提交预期文件；`.codegraph/`、索引文档和其他无关未跟踪文件除非明确要求不得删除或纳入提交。

## 5. 服务器与凭据安全

- PEM、密码、令牌、Cookie、AWS 凭据和代理认证信息只在本地或服务器安全配置中使用，不写入源码、日志、提交信息或文档，不在回复中回显明文。
- SSH、AWS、SSM、代理和浏览器连接先做只读认证/连通性检查，再执行变更。
- 使用代理前确认代理协议、监听地址、作用范围和出口是否满足业务需求；不得默认把桌面 VPN 配置当作服务器代理。
- 数据库重置、删除、迁移和覆盖属于不可逆或高风险操作，必须再次确认目标环境、备份状态和影响范围。

## 6. 交付记录

每次交付至少记录：

1. 修改内容和涉及文件；
2. 使用的 AI_API 基线/分支及参考项目版本；
3. 构建、测试、健康检查和浏览器验证结果；
4. 提交哈希、推送目标和部署状态；
5. 未完成验证、已知风险和回滚方式。

任何后续工作都应以本规范和当前 AI_API 代码为准，避免再次把参考项目误当成线上主体。
